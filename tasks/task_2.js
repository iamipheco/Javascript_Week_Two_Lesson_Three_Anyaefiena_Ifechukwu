// QUestion
// Use a "for" loop to iterate in reverse
// Create an array of strings.
// Use a "for" loop to iterate over the array in reverse and log each string to the console.

const fruitsStr = ['Apple', 'Pineapple', 'Orange', 'Banana', 'Grape']
for (let i = fruitsStr.length - 1; i >= 0; i--) {
  console.log(fruitsStr[i])
}
